---
title: "Parks in Syracuse"
output: github_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE,  warning = F , message = F)
```


```{}
#Load Packages
library( dplyr )
library( geojsonio )
library( ggmap )
library( maps )
library( maptools )
library( raster )
library( rgdal )
library( rgeos )
library( sp )
```



```{r}
setwd("C:/Users/mgao05/Desktop/PAI INdenpendent study/park")


#Load Onondaga Data in Environment
ono_tracts <- readShapePoly( fn="tl_2010_36067_tract10", proj4string=CRS("+proj=longlat +datum=WGS84") )
syr <- ono_tracts[as.numeric(as.character(ono_tracts$NAME10)) < 62, ]


###load park shapefiles

park <- readShapePoly( fn="SyracuseParks051414_UTM", verbose=TRUE,proj4string=CRS("+proj=longlat +datum=WGS84") )

syr <- readShapePoly( fn="tl_2010_36067_tract10", proj4string=CRS("+proj=longlat +datum=WGS84") )

library(rgdal)
library(sp)
library(maptools)

# write geojson
#geojson_file <- "C:/Users/mgao05/Desktop/PAI INdenpendent study/park/park.geojson"
#writeOGR(park, geojson_file,layer = "geojson", driver = "GeoJSON")


plot(syr, border="gray10")
plot(park, border="gray10")
dat <- read.csv("C:/Users/mgao05/Desktop/PAI INdenpendent study/park/public_park.csv", sep = ",", header=TRUE )

dat.park <- dat[dat$LandUse == "Parks", ]
tbl.nhood <- table(dat.park$Nhood)
tbl.nhood



plot(dat.park$ACRES)

#seperate parks of different areas
small.size.park <- dat.park[dat.park$ACRES <= 1, ]

medium.size.park <- dat.park[dat.park$ACRES <= 10 & dat.park$ACRES > 1, ]

large.size.park <- dat.park[dat.park$ACRES <= 150 & dat.park$ACRES >10, ]

#seperate by neighborhoods function since parks in different neighborhood might have different effect on house value

sep.nhood <- function(Nhood){
  dat <- dat.park[dat.park$Nhood == Nhood, ]
  return(dat)
}

#test on south valley
South.Valley <- sep.nhood("South Valley")
Southside <- sep.nhood("Southside")






```


